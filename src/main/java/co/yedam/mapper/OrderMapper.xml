<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yedam.mapper.OrderMapper">

    <!-- 주문 정보 삽입 -->
    <insert id="insertOrder" parameterType="order">
        INSERT INTO order_info (payment_uid, 
                            od_no,
                            od_status, 
                            od_price,
                            od_total,
                            use_mali, 
                            re_name, 
                            re_phone, 
                            re_addr, 
                            del_fee, 
                            user_id, 
                            od_date)
                            
        VALUES (#{paymentUid},
                #{odNo},
                #{odStatus}, 
                #{odPrice},
                #{odTotal},
                #{useMali}, 
                #{reName}, 
                #{rePhone}, 
                #{reAddr}, 
                #{delFee}, 
                #{userId}, 
                sysdate)
    </insert>
    
    <!-- 사용자 ID로 주문 목록 조회 -->
    <select id="getOrderListByUserId" parameterType="string" resultType="co.yedam.vo.OrderVo">
        SELECT
            od_no,
            payment_uid,
            od_status,
            od_price,
            od_total,
            use_mali,
            re_name,
            re_phone,
            re_addr,
            del_fee,
            user_id,
            od_date
        FROM
            order_info
        WHERE
            user_id = #{userId}
        ORDER BY
            od_date DESC  <!-- 최신 주문부터 표시 -->
    </select>
    
    <select id="getOrder" parameterType="string" resultType="co.yedam.vo.OrderVo">
        SELECT 
            od_no,
            payment_uid,
            od_status,
            od_price,
            od_total,
            use_mali,
            re_name,
            re_phone,
            re_addr,
            del_fee,
            user_id,
            od_date
        FROM order_info
        WHERE od_no = #{odNo} 
          AND user_id = #{userid}
    </select>

</mapper>
