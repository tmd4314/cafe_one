<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yedam.mapper.MemberMapper">
	<select id="selectMember" resultType="co.yedam.vo.MemberVO">
		SELECT user_id,
		user_pw,
		user_name,
		phone,
		address,
		email,
		mailage,
		review_id,
		order_detail_no
		FROM user_info
		WHERE user_id = #{id}
		AND user_pw = #{pw}
	</select>
	<!-- 아이디로 회원 조회 -->
	<select id="selectMemberId" resultType="co.yedam.vo.MemberVO"
		parameterType="string">
		select *
		from user_info
		where user_id = #{id}
	</select>
	<insert id="insertMember" parameterType="co.yedam.vo.MemberVO">
		INSERT INTO user_info (user_id, user_pw, user_name)
		VALUES (#{userID}, #{userPw}, #{userName},)
	</insert>
	<update id="updatePasswd" parameterType="co.yedam.vo.MemberVO">
		update user_info
		set user_pw = #{userPw}
		where user_id = #{userID}
	</update>
</mapper>