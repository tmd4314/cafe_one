<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.yedam.mapper.ReviewMapper">
   	 
   	 <select id="reviewList" resultType="co.yedam.vo.ReviewVO">
   	 	SELECT *
   	 	FROM review
   	 	WHERE PD_CODE = #{pdc}
   	 	ORDER BY review_id DESC
   	 </select>
   
     <insert id="insertReview" parameterType="co.yedam.vo.ReviewVo">
		INSERT INTO review (
		    review_id, review_title, review_content, user_id, pdCode
		) VALUES (
		    #{pdc},
		    (SELECT NVL(MAX(review_id), 0) + 1 FROM review WHERE pd_code = #{pdc}),
		    #{rt},
		    #{rc},
		    #{uid},
		    SYSDATE
		);
   	 </insert>
</mapper>